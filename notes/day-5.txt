TypeScript:
---------------
superset of JavaScript
development mode language
static type checking
strict typing


TS -> convert -> JS [transpiler -> TSC]

Note: extra features of TS (such as, interface, type, generic, extra data types [any, unknown etc.]) ae NOT converted to JS during transpilation

install typescript:
-----------------------------
>npm i typescript -D

create tsconfig.json file:
-----------------------------
>.\node_modules\.bin\tsc --init

"use strict"

Reat with TypeScript:
----------------------------
StrictMode: 
a built-in component. 
used during development time and wraps the whole react app (root component App)
Lets you find common bugs in your components early during development.

<Header messageValue={message} />

Header({messageValue:message})

type HeaderPropsType={
	messageValue:string
}

const Header = (props:HeaderPropsType)=>{
	return (<h2>{props.messageValue}</h2>)
}

create react app using vite:
------------------------------
>npm create vite@latest
	- enter proj name
	- enter tech [react/preact/...]
	- enter flavor [Ts/Js/..]
	- decide to install packages immediately [yes/no]
	- ...

running React app using vite:
-----------------------------------
app-dir>npm i
app-dir>npm run dev [means: npx vite --port=5173]

or

app-dir>npx vite --port=4000




code:
------------------------

import './App.css'

const App = () => {
  let count = 0

  const increaseHandler = () => {
    console.log(count);
    count = count + 1
    console.log(count);
  }
  return (
    <div>
      Count: &nbsp; {count}
      <br />
      <button type="button" onClick={increaseHandler}> Increase</button>
    </div>
  )
}

export default App

error: 
-------------------------
Error: Cannot modify local variables after render completes

This argument[increaseHandler] to onClick is a function which may reassign or mutate `count` after render, which can cause inconsistent behavior on subsequent renders. Consider using "state" instead.


conclusion: any data after render should not be modified
if you want React to track the changes in the data, DO NOT declare the data as local variable of the function
create a state for the component and save value
when the state is updated, the component will be recalled by React producing new version of the component's VDOM which updates the UI


function useState<T>(initialState:T){
	let state = initialState //0
	
	function setState(newState:T=1){
		state = newState//1
	}

	return [state, setState]
}

	
